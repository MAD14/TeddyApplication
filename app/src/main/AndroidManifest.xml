<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.polito.teddyapplication">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".RegistrationActivity" />
        <activity
            android:name=".NewGroupActivity"
            android:label="@string/title_activity_new_group"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
<<<<<<< HEAD
            android:name=".GroupActivity"
            android:label="@string/title_activity_group"></activity>
        <activity
            android:name=".ExpenseCreation"
            android:label="@string/title_expense_creation"></activity>
        <activity
=======
>>>>>>> origin/master
            android:name=".ChoiceGroupMembersActivity"
            android:label="@string/title_activity_choice_group_members"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>